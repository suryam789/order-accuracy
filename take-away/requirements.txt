minio==7.2.9
opencv-python-headless
numpy
fastapi
uvicorn
requests
pyyaml
python-multipart
pika>=1.3.0
psutil
# EasyOCR with dependencies (torch is required by easyocr)
easyocr
torch
torchvision
# ultralytics: YOLO table-presence detection in frame_pipeline.py
# Uses the INT8 OpenVINO model (yolo11n_int8_openvino_model) so PyTorch is only
# needed for ultralytics bookkeeping, not for inference.
ultralytics==8.3.0
# openvino is already in the base image (intel/dlstreamer:2025.2.0-ubuntu22).
# openvino-genai is NOT needed — VLM inference uses OVMS (external service),
# not local openvino_genai.VLMPipeline. The semantic_matcher.py fallback
# gracefully handles the missing import.
git+https://github.com/intel-retail/performance-tools.git@main#subdirectory=benchmark-scripts